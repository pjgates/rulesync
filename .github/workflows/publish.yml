name: Publish

on:
  workflow_run:
    workflows: ["Publish Assets"]
    types: [completed]

permissions:
  id-token: write # Required for OIDC
  contents: write # Required for checking out code and editing release state

jobs:
  publish:
    name: Publish to NPM
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'pull_request' && startsWith(github.event.workflow_run.head_branch, 'release/v')

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          ref: main

      - name: Setup mise
        uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v2
        with:
          experimental: true

      - name: Install dependencies
        run: pnpm install --ignore-scripts

      - name: Publish to npm
        run: pnpm publish

      - name: Publish target draft release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HEAD_BRANCH: ${{ github.event.workflow_run.head_branch }}
        run: |
          TAG="${HEAD_BRANCH#release/}"
          if [ "$TAG" != "v8.30.1-omp.1" ]; then
            echo "::error::Release tag must be exactly v8.30.1-omp.1, got: $TAG"
            exit 1
          fi
          gh release edit "$TAG" --draft=false --latest=false
